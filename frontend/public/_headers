# DEPRECATED — the project moved from Cloudflare Pages to Vercel (2026-09-12).
# This Cloudflare-Pages-specific header file has no effect on Vercel and is kept
# only because the sandbox's destructive-action guard blocked deleting it; the
# headers now live in `public/vercel.json`. Safe to `git rm frontend/public/_headers`
# whenever convenient.
