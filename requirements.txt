# Core dependencies with pinned versions for Streamlit Cloud
streamlit==1.32.0
duckdb==0.10.0
polars==0.20.10
pandas==2.2.0
pyarrow==15.0.0
plotly==5.19.0
scikit-learn==1.4.2

# Optional: only if using ML models
# lightgbm==4.3.0

# Configuration management (local development only, not strictly required for cloud)
# hydra-core==1.3.2