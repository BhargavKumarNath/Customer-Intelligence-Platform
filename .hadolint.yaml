# hadolint config — see .github/workflows/ci.yml `security` job.
# Any hadolint finding not listed below still fails CI.
ignored:
  # DL3013 / DL3008: pin pip / apt versions. The base image is now digest-pinned
  # (Phase 1 task 4) and the app's own dependencies install from the hash-pinned
  # requirements.lock, so the reproducibility gap these target is already closed.
  # The two remaining unpinned installs are `libgomp1` (a single stable Debian
  # runtime lib) and a `pip/setuptools/wheel` upgrade in the throwaway builder
  # venv — pinning either to an exact patch would just create churn with no
  # supply-chain benefit here.
  - DL3013
  - DL3008
  # DL3025: HEALTHCHECK CMD is shell-form on purpose (a one-liner python probe);
  # the container's own CMD already uses JSON exec form.
  - DL3025
  # DL3066: `useradd --system` without an explicit numeric UID. Cosmetic here
  # (single-tenant image, non-root is what matters); revisit with the digest pin.
  - DL3066
